class User {

  constructor(name, email, birthDate, isActive) {

    this.name = name;
    this.email = email
    this.birthDate = birthDate
    this.isActive = isActive
  }
}

module.exports = User